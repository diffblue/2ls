int main()
{
 int i = 0, x=0, y=0, z=0;
    if(x>=0)
     x++;
    else
     x--;
 /*   if(y>=0)
     y++;
    else
     y--;
    if(z>=0)
     z++;
    else
     z--;*/
 //assert(((x+y+z) < 0));
 assert(x >= 0);
}
